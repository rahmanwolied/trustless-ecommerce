model Product {
    id String @id @default(cuid())

    name String
    price Float
    rating Float
    purchases Int
    image String

    sellerId String
    seller Seller @relation(fields: [sellerId], references: [id])
}
